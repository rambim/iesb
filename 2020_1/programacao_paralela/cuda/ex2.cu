#include <stdio.h>
#include <stdlib.h>
#include <cuda.h>
#include <cuda_runtime.h>

__global__ void funcao()
{


}

int main()
{
    //declaração de todas variáveis

    // alocação de memória principal (host)

    // Alocação dinâmica de memória para ser utilizada na GPU.

    // Carrega variáveis no host.

    // Copia conteúdo da variável host pra variável da GPU.

    // executa kernel (função) na GPU

    // Copia os resultados de processamento na GPU de volta pra variável host.

    // desaloca memória do host

    // desaloca memória da GPU.

    return 0;
}