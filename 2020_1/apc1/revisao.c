/*
    O que é um algoritmo?
        sequencia de passos, ordenado, finito que resolve um problema. 

    O que é uma variável?
        Espaço de memória

    O que é declarar uma variável?
        Reservar espaço de memória.

    Quais são os atributos de uma variável em C?
        Nome
        Tipo
        Conteúdo
        Endereço.

    Quais as formas de representação de um algorítmo?
        fluxograma
        narrativo
        pseudocódigo.

    Como construir um algoritmo passo a passo.
        Verificar quais dados de entrada possui ( definir dados de entrada)
        Verificar saída necessária ( definir os dados de saída);
        Transformar os dados de entrada no dado esperado da saída ( processamento da informação)

    Operadores Lógicos.
        
            = ( atribuição)
            ! ( negação)
            == ( comparação de igualdade)
            != ( comparação de diferença)

            <  (menor que)
            >  (maior que)
            >= (maior ou igual)
            <= (menor ou igual)

            && ( and = e)
            || ( or )

        

    Tipos de variáveis.
        int , float, char;

        
    Atribuições

    Comandos de Leitura e Escrita.

    Estruturas Condicionais

    Estruturas de repetição

    Vetor.

    Funções.

*/